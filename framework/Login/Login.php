<?php

require_once FRAME_SQL;

class Login {

/**
 * Crèe une session si les identifiant on correct.
 * @param type $s_login login
 * @param type $s_mdp mot de passe
 * @return boolean retourne vrai ou faux en fonction des identifiant passé en paramètre
 */
    public function login_site($s_login, $s_mdp) {
        
       return true;
    }

}
